services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    restart: unless-stopped
    env_file:
      - ./api/rackforge.env
    ports:
      - "8080:8080"
    volumes:
      - rackforge-data:/data

volumes:
  rackforge-data:
